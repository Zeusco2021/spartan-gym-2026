server:
  port: 8082

spring:
  application:
    name: servicio-gimnasio
  datasource:
    url: jdbc:postgresql://localhost:5432/spartan_gimnasio
    username: spartan
    password: spartan
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  redis:
    host: localhost
    port: 6379
  kafka:
    bootstrap-servers: localhost:9092
