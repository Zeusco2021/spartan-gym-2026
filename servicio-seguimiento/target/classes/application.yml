server:
  port: 8084

spring:
  application:
    name: servicio-seguimiento
  redis:
    host: localhost
    port: 6379
  kafka:
    bootstrap-servers: localhost:9092

aws:
  dynamodb:
    endpoint: http://localhost:8000
    region: us-east-1
